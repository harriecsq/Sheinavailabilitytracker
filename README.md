
  # Create app from file

  This is a code bundle for Create app from file. The original project is available at https://www.figma.com/design/qkjLS7Cgcfv53p1lpSlM7F/Create-app-from-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  